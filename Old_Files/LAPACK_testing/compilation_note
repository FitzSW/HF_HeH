The files in this directory are compiled with the following shell function:

ff () {
	gfortran -L/usr/local/opt/lapack/lib -o temp $1 -llapack -lblas -mmacosx-version-min=10.14 && ./temp && rm temp
}

The path given above is system specific and will vary based on LAPACK
installation. 

Tutorial Link:
https://cyber.dabamos.de/programming/modernfortran/lapack.html
